<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DeleteTimerConfigurationActivity"
    android:background="@color/bg">

    <ImageButton
        android:id="@+id/back_button"
        android:layout_width="61sp"
        android:layout_height="57sp"
        android:contentDescription="Back button"
        android:backgroundTint="@android:color/transparent"
        android:backgroundTintMode="src_in"
        android:scaleType="fitXY"
        android:layout_marginTop="15sp"
        android:layout_marginStart="15sp"
        app:srcCompat="@drawable/back" />

    <TextView
        android:id="@+id/delete_configuration_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Удаление конфигурации таймера"
        android:fontFamily="@font/montserrat_medium"
        android:textSize="20sp"
        android:textColor="@color/white"
        android:layout_marginTop="10sp"
        android:layout_marginBottom="30sp"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/back_button"/>

    <ListView
        android:id="@+id/list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/delete_configuration_title"
        android:layout_marginHorizontal="25sp"
        android:layout_marginBottom="20sp"
        android:textColor="#FFFFFF" />

</RelativeLayout>