<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PomodoroApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="SCREEN">
        <item name="android:background">@color/dark_red</item>
    </style>

    <style name="TEXT">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BUTTON">
        <item name="android:backgroundTint">@color/dark_theme</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="HEADER">
        <item name="android:background">@color/dark_theme</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="SMALLCIRCLE">
        <item name="android:backgroundTint">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="CIRCLE">
        <item name="android:backgroundTint">@color/dark_red</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="BIG_CIRCLE">
        <item name="android:backgroundTint">@color/black</item>
    </style>

</resources>